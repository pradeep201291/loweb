/**
 * 
 * 
 * @export
 * @class LoginResponse
 */
export class LoginResponse {
    access_token: string;
    token_type: string;
    expires_in: number;
    refresh_token: string;
    UserName: string;
    UserType: string;
    issued: string;
    expires: string;
}
